---
phase: 02-signal-rack-dashboard-redesign
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/dashboard/v3Input.ts
  - tests/dashboard.test.mjs
autonomous: true
requirements: [DASH-10]
must_haves:
  truths:
    - "Pressing x on a selected peer enters a kill-confirm mode (not a modal); enter confirms the kill, esc cancels"
    - "Pressing a on a waiting peer enters an answer mode (not a modal); typed text submits via sendPeerReply, esc cancels"
    - "Answer text is .trim()-ed and empty replies are rejected before reaching sendPeerReply"
    - "No V3 code path sets or reads state.mode === 'modal' / 'modal-kill' / 'modal-answer' anymore"
  artifacts:
    - path: "src/dashboard/v3Input.ts"
      provides: "V3Mode with kill-confirm/answer status-line modes; retired modal routing; preserved answer input validation"
      contains: "kill-confirm"
    - path: "tests/dashboard.test.mjs"
      provides: "Updated assertions for status-line kill-confirm/answer transitions and preserved trim/empty-reject"
      contains: "kill-confirm"
  key_links:
    - from: "handleDashboardV3Input submit-answer"
      to: "actions.sendPeerReply"
      via: "trimmed, empty-rejected answerInput"
      pattern: "sendPeerReply"
---

<objective>
Retire V3's modal-based kill/answer flow and replace it with the status-line modes DASH-10 (and the sketch)
require. `model.ts` already models the correct shape (`DashboardMode = "normal" | "kill-confirm" | "answer" | "help"`);
`v3Input.ts` diverged into `modal`/`modal-kill`/`modal-answer`/`palette`. This plan converges V3's mode union
and key routing so `x` → kill-confirm and `a` → answer render inline (the actual rendering lands in Plan 05),
while preserving the existing V5 input-validation control on answer text.

Purpose: Close the input half of DASH-10 and make the mode transitions unit-testable (per 02-VALIDATION.md).
Output: Reworked `v3Input.ts` mode machine + updated `tests/dashboard.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-RESEARCH.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-VALIDATION.md
@.claude/skills/sketch-findings-delamain/references/keyboard-and-detail-dock.md
@src/dashboard/v3Input.ts
@src/dashboard/model.ts
@tests/dashboard.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retire modal modes; add status-line kill-confirm and answer modes</name>
  <files>src/dashboard/v3Input.ts, tests/dashboard.test.mjs</files>
  <read_first>
    - src/dashboard/v3Input.ts lines 8-9 (V3Mode union), 115-197 (v3CommandForKey mode switch), 199-265 (normalCommand: x→open-modal-kill, a→open-modal-answer), 267-439 (handleDashboardV3Input command routing), 553-674 (openModal/openAnswer/enterModalAnswer/modalKill/submitAnswer/closeModal/cancel)
    - src/dashboard/model.ts line 8 (DashboardMode target shape) and 289-303 (messageForState produces the kill-confirm/answer text)
    - .claude/skills/sketch-findings-delamain/references/keyboard-and-detail-dock.md line 13 (kill-confirm shows `kill <id>? ↵ confirm · esc cancel`; answer shows inline `reply → <id>: [input]`)
    - 02-RESEARCH.md Pitfall 2 (retire modal/modal-kill/modal-answer, replace with model.ts-shaped kill-confirm/answer) + Deprecated note (palette out of scope, keep)
  </read_first>
  <behavior>
    - normal + x on a selected peer → state.mode === "kill-confirm", state.modalPeerId (or a renamed pendingPeerId) set to the selection
    - kill-confirm + enter → actions.killPeer(id) then back to normal; kill-confirm + esc → normal, no kill
    - normal + a on a waiting peer → state.mode === "answer", answerInput === ""; a on a non-waiting peer → toast, mode stays normal
    - answer + printable keys append to answerInput; backspace deletes; enter → submit; esc → normal, answerInput cleared
    - After the rework, no reachable state has mode "modal", "modal-kill", or "modal-answer"
  </behavior>
  <action>
    Change `V3Mode` to `"normal" | "kill-confirm" | "answer" | "palette" | "help"` (keep palette per the Deprecated note —
    out of scope to remove). In `v3CommandForKey`, delete the `modal` / `modal-kill` / `modal-answer` case blocks and add
    `case "kill-confirm"` (enter → a new `confirm-kill` command, esc/q → `cancel`) and `case "answer"` (enter → `submit-answer`,
    esc → `cancel`, printable handled by the existing text-input branch). In `normalCommand`, remap `x` → a new `open-kill-confirm`
    command and `a` → `open-answer`. In `handleDashboardV3Input`, replace the `open-modal`/`open-modal-kill`/`open-modal-answer`/
    modal-* cases with handlers that set `state.mode = "kill-confirm"` / `"answer"` directly (reusing the selected-peer guard from
    `openModal`/`openAnswer`, including the waiting-only check for answer and the "No peer selected" toast). Point the palette peer/
    kill/answer entries at select + kill-confirm / answer instead of `openModal`. Retire `open-modal`, `open-modal-kill`,
    `open-modal-answer`, and all `modal-*` command union members and helpers (openModal, modalButtons, moveModalButton,
    activateModalButton, enterModalAnswer, modalViewLog, modalKill, modalTab/modalButton/modalScroll state) that become dead.
    Update the existing tests at lines ~370-464 (modal-mode, modal-answer, modal-kill, "x opens modal-kill") to assert the new
    status-line transitions and mode names. NOTE: the `↵`/space "open details" key in normal mode no longer opens a modal — the
    bottom dock (Plan 05) always shows the selection, so map `↵`/space to a no-op or keep it selecting the peer; do not reintroduce a modal.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/dashboard/v3Input.ts | grep -c '"modal"' ` returns 0 (no live "modal" mode literal)
    - A test asserts x → mode "kill-confirm" and enter → killPeer called, esc → mode "normal" with no kill
    - A test asserts a on a waiting peer → mode "answer"; a on a non-waiting peer → mode stays "normal" and a toast is pushed
    - `npx tsc -p tsconfig.json --noEmit` and `node --test tests/dashboard.test.mjs` pass
  </acceptance_criteria>
  <done>V3 kill/answer are status-line modes matching model.ts's DashboardMode; the modal machine is gone and transitions are unit-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve answer input validation through the rework</name>
  <files>src/dashboard/v3Input.ts, tests/dashboard.test.mjs</files>
  <read_first>
    - src/dashboard/v3Input.ts lines 639-656 (submitAnswer: `.trim()` + empty-reject + sendPeerReply) and 269-277 (text-input append/backspace branch keyed on mode)
    - 02-RESEARCH.md Security Domain V5 (keep `.trim()` + empty-string rejection when moving out of the modal) + threat_model_requirement (carry forward control V5)
  </read_first>
  <behavior>
    - submit-answer with only whitespace → no sendPeerReply call, an "empty reply" toast, mode returns toward normal
    - submit-answer with "  hi  " → sendPeerReply called with "hi" (trimmed)
    - The text-input append/backspace branch fires when state.mode === "answer" (updated from the old "modal-answer")
  </behavior>
  <action>
    Update the text-input guard in `handleDashboardV3Input` (currently `state.mode === "modal-answer"`) to `state.mode === "answer"`.
    Keep `submitAnswer`'s `const text = state.answerInput.trim()` and the empty-string rejection exactly as-is; only change how the
    target peer id is resolved if you renamed `modalPeerId`. Add/keep tests: one asserting a whitespace-only answer does NOT call
    sendPeerReply and pushes a toast; one asserting a padded answer calls sendPeerReply with the trimmed string. Preserve the existing
    "typed answer submits through sendPeerReply" test intent (line ~436), updated to the new mode name.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.trim()" src/dashboard/v3Input.ts` is &gt;= 1 and covers the answer path
    - A test proves a whitespace-only reply is rejected (sendPeerReply not called) with a toast
    - A test proves a padded reply reaches sendPeerReply trimmed
    - Full `node --test tests/dashboard.test.mjs` passes
  </acceptance_criteria>
  <done>The V5 input-validation control (trim + empty-reject) survives the modal→status-line move, proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator keyboard → answer text → resumePeer | Free-text reply crosses into peer control (`sendPeerReply` → `resumePeer`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Tampering (ASVS V5) | submitAnswer answer text | mitigate | Preserve `.trim()` + empty-string rejection when moving answer input out of the modal (Security Domain V5); reply never reaches sendPeerReply empty |
| T-2-03 | Tampering | kill target peer id | accept | kill-confirm resolves the id from server-refreshed state.visiblePeers; killPeer validates server-side (peerManager, out of scope) — no new surface vs the retired modal |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` clean
- `npm run build && node --test tests/dashboard.test.mjs` green
- No live reference to modal/modal-kill/modal-answer modes remains
</verification>

<success_criteria>
- x → kill-confirm, a → answer render as status-line modes (input side), modal machine retired
- Answer text validation (trim + empty-reject) preserved and proven
- Mode transitions unit-tested; full suite passes
</success_criteria>

<output>
Create `.planning/phases/02-signal-rack-dashboard-redesign/02-03-SUMMARY.md` when done.
</output>
