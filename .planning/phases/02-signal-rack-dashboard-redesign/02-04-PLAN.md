---
phase: 02-signal-rack-dashboard-redesign
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/dashboard/opentuiV3.ts
autonomous: false
requirements: [DASH-09]
must_haves:
  truths:
    - "The fleet route renders peers grouped by status in triage order WORKING → WAITING → STARTING → FAILED → DONE, one dense monospace row each"
    - "Each row shows selection caret ▶ + status glyph, teal id, engine chip, branch/activity, an inline 10-cell context meter colored by contextLevel, a ⛁ flag when compacted, and right-aligned dim elapsed"
    - "A peer with no context telemetry shows a dim placeholder meter (no percent), not a 0%/green bar"
    - "A fleet header shows the DELAMAIN ▍FLEET logo (orange, glow), per-status count chips, and a teal codex 5h usage meter"
  artifacts:
    - path: "src/dashboard/opentuiV3.ts"
      provides: "Triage-grouped rack rows with inline context meters + restyled fleet header"
      contains: "triageGroups"
  key_links:
    - from: "opentuiV3.ts rack rows"
      to: "model.ts triageGroups / contextMeterCells / contextLevelColor"
      via: "imported helpers"
      pattern: "contextMeterCells"
---

<objective>
Rework the fleet route's roster into the sketch-locked Signal Rack: consume Plan 01's `triageGroups` for the
5-bucket triage order, restyle each row into the sketch's dense column layout with an inline context-window block
meter (`contextMeterCells` + `contextLevelColor`, skull-blink, `⛁` compacted flag, dim placeholder when unmeasured),
and restyle `appBar` into the fleet header (DELAMAIN ▍FLEET logo + per-status count chips + codex 5h usage meter).
This is a rework-in-place of `STATUS_ORDER`/`STATUS_GLYPH`/`rosterLines`/`rosterPane`/`rosterLineChunks`/`appBar`.

Purpose: Deliver DASH-09's visible surface (Success Criteria 1 and the header half of 2).
Output: Reworked rack + header in `opentuiV3.ts`. Verified by type-check, full suite, and a human visual check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-RESEARCH.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-VALIDATION.md
@.claude/skills/sketch-findings-delamain/references/layout-and-density.md
@.claude/skills/sketch-findings-delamain/sources/002-signal-rack-terminal/index.html
@src/dashboard/opentuiV3.ts
@src/dashboard/model.ts
@src/dashboard/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage-grouped rack rows with sketch glyphs</name>
  <files>src/dashboard/opentuiV3.ts</files>
  <read_first>
    - src/dashboard/opentuiV3.ts lines 40-56 (STATUS_ORDER — current 15-status order), 71-88 (STATUS_GLYPH), 463-546 (RosterLine/rosterLines/rosterPane grouping + scroll + selection follow)
    - src/dashboard/model.ts triageGroups/triageBucketForStatus (added in Plan 01)
    - .claude/skills/sketch-findings-delamain/references/layout-and-density.md lines 5-7, 39-44 (triage order, group rule line `▾ WORKING 2 ─────`, status glyphs: working ◉, waiting ◍ blinking, starting ◌, done ●, failed ✖)
    - 02-RESEARCH.md Pattern 1 + Assumption A1 (fold GSD statuses; keep 5 buckets)
  </read_first>
  <action>
    Replace the roster's grouping source: drive `rosterLines` (rename toward `rackLines` if clearer) from Plan 01's
    `triageGroups(view.peers)` so groups render in the 5-bucket triage order with a group-rule header line
    (`▾ WORKING 2 ─────`) and the existing collapse/`c` behavior keyed by bucket. Retire the special-cased "terminal" single-line
    collapse (done/killed/idle/... folded into one strip) in favor of a real DONE bucket with a count. Update `STATUS_GLYPH`
    entries for the five named statuses to the sketch glyphs (working ◉, waiting ◍, starting ◌, done ●, failed ✖); keep GSD/other
    statuses mapping to their folded bucket's glyph. Preserve the existing peerOffset/followSelectedPeer scroll math and
    selection clamping in `rosterPane` — only the grouping/labels change, not the scroll mechanics.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "triageGroups" src/dashboard/opentuiV3.ts` returns &gt;= 1
    - The rack renders at most 5 group headers, ordered WORKING, WAITING, STARTING, FAILED, DONE (buckets present only if non-empty)
    - `c` still collapses/expands the selected peer's bucket; peer selection follow-scroll still works
    - `npx tsc -p tsconfig.json --noEmit` and the full suite pass
  </acceptance_criteria>
  <done>The fleet route groups peers into the 5 sketch buckets in triage order with the sketch's glyphs and group rules.</done>
</task>

<task type="auto">
  <name>Task 2: Inline context-window block meter per row</name>
  <files>src/dashboard/opentuiV3.ts</files>
  <read_first>
    - src/dashboard/opentuiV3.ts lines 548-612 (rosterLineChunks — current row column layout, selection highlight, padEnd math)
    - src/dashboard/model.ts contextMeterCells/contextLevelColor + DashboardPeerRow.contextPercent/contextLevel/compacted (Plan 01)
    - .claude/skills/sketch-findings-delamain/references/layout-and-density.md lines 6-8 (row contents left→right; 10-cell meter `████░░░░░░ 42%`; ⛁ compacted flag)
    - 02-RESEARCH.md Pattern 2 (skull blink via `Math.floor(nowMs / 480) % 2`) + Pitfall 3 (undefined → dim placeholder, never 0%/green)
  </read_first>
  <action>
    In `rosterLineChunks` (rack row), insert the context meter column between activity and elapsed per the sketch order:
    caret `▶` + glyph, teal id, engine chip (cursor engine → purple `#c084fc`, codex → theme accent), branch/activity, the
    context meter, `⛁` when `peer.compacted`, right-aligned dim elapsed. Build the meter with `contextMeterCells(peer.contextPercent)`
    colored by `contextLevelColor(peer.contextLevel, theme)` plus a ` NN%` suffix; when `peer.contextPercent === undefined` render a
    dim placeholder (e.g. `──────────` with no percent), NOT a 0%/green bar (Pitfall 3). When `peer.contextLevel === "skull"`, blank the
    bar on alternating frames using `Math.floor(nowMs / 480) % 2 === 0` to blink. Keep the existing selected-row background/foreground
    treatment and the `applyBg` sweep; recompute the column-width bookkeeping (`usedSoFar`/`eventWidth`) so alignment stays character-exact.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - A measured peer row includes a 10-cell `█`/`░` meter with a percent suffix colored by contextLevel
    - An unmeasured peer (contextPercent undefined) renders a dim placeholder with no percent and no green bar
    - A compacted peer row includes the `⛁` glyph
    - `npx tsc -p tsconfig.json --noEmit` and the full suite pass
  </acceptance_criteria>
  <done>Every rack row carries an undefined-safe, level-colored inline context meter and compacted flag matching the sketch.</done>
</task>

<task type="auto">
  <name>Task 3: Fleet header — logo, count chips, codex usage meter</name>
  <files>src/dashboard/opentuiV3.ts</files>
  <read_first>
    - src/dashboard/opentuiV3.ts lines 306-337 (appBar: existing PEERS/ACTIVE/WAITING/FAILED pill chips) and 729-790 (limitsContent/brailleMeterLine/usageLevelColor — existing codex usage meter rendering to reuse)
    - .claude/skills/sketch-findings-delamain/references/layout-and-density.md lines 9-10, 36-37 (`DELAMAIN ▍FLEET` orange glow logo + per-status count chips + codex 5h usage meter teal)
    - src/dashboard/theme.ts (accent orange #ff7a1a, borderFocused/accent teal #35e0d8, statusColors)
    - 02-RESEARCH.md DASH-09 row + Don't Hand-Roll (reuse view.counts and view.codexUsage; extend appBar, don't rebuild)
  </read_first>
  <action>
    Restyle `appBar` into the fleet header: left side `DELAMAIN ▍FLEET` in theme accent orange with the existing glow treatment;
    middle/right per-status count chips built from `view.counts` mapped through the 5 triage buckets (working/waiting/starting/failed/done
    counts, each with its status glyph + color), plus a codex 5h usage meter rendered in teal by reusing the existing
    `brailleMeterLine`/`usageLevelColor` helpers against `view.codexUsage` (the primary/5h window). Keep the `: palette` hint if palette
    stays. Do not add new theme colors — use cyberpunkTheme keys. Keep the header at its current height/border treatment so `computeLayout`
    is unaffected.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The header renders `DELAMAIN ▍FLEET` in the accent color
    - The header shows per-status count chips derived from view.counts across the 5 triage buckets
    - The header shows a codex usage meter driven by view.codexUsage in teal
    - `npx tsc -p tsconfig.json --noEmit` and the full suite pass
  </acceptance_criteria>
  <done>The fleet header presents the sketch logo, per-status counts, and a codex usage meter, all in cyberpunkTheme.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual check — rack rows + fleet header</name>
  <read_first>
    - .claude/skills/sketch-findings-delamain/sources/002-signal-rack-terminal/index.html (canonical mock — winner B default view)
    - 02-VALIDATION.md Manual-Only Verifications (palette/glyph fidelity has no headless assertion)
  </read_first>
  <what-built>
    Triage-grouped Signal Rack rows with inline context meters + compacted flags, and a restyled fleet header with the
    DELAMAIN ▍FLEET logo, per-status count chips, and a codex usage meter — all in cyberpunkTheme.
  </what-built>
  <how-to-verify>
    1. Run `CODEX_PEERS_DASHBOARD_SMOKE=1 bun src/dashboard/bunEntryV2.js` (or an interactive `bun` dashboard run) with at least one codex peer.
    2. Confirm peers group under WORKING → WAITING → STARTING → FAILED → DONE headers in that order, one dense row each.
    3. Confirm each measured row shows a 10-cell block meter with a percent, colored green/yellow/red/skull; a skull peer blinks; an unmeasured peer shows a dim placeholder (no percent); compacted peers show ⛁.
    4. Confirm the header shows DELAMAIN ▍FLEET (orange, glow), per-status count chips, and a teal codex usage meter — sharp corners, scanlines, no rounded/soft styling.
    5. Compare against sources/002-signal-rack-terminal/index.html.
  </how-to-verify>
  <acceptance_criteria>
    - Groups appear in the exact triage order with sketch glyphs
    - Context meters are colored by level, blink on skull, and never misrepresent an unmeasured peer as green
    - Header matches the sketch logo/chips/usage-meter and cyberpunkTheme aesthetic
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer telemetry → rack row render | Untrusted peer id/branch/context values rendered as terminal text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | context meter render | mitigate | Undefined telemetry renders a dim placeholder, never a green 0% bar (Pitfall 3) |
| T-2-04 | Tampering/Info Disclosure | peer-supplied text (id, branch, activity) in rows | accept | Pre-existing — OpenTUI Text/StyledText handles escaping; same data path as today, no new surface (Security Domain) |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` clean
- `npm run build && node --test tests/dashboard.test.mjs` green (regression guard for Success Criterion 6)
- Human visual check (Task 4) approved against sources/002 mock
</verification>

<success_criteria>
- Fleet route renders 5-bucket triage-grouped dense rows with inline, level-colored, undefined-safe context meters and ⛁ flags
- Fleet header shows DELAMAIN ▍FLEET logo, per-status count chips, and codex usage meter in cyberpunkTheme
- Existing suite still passes
</success_criteria>

<output>
Create `.planning/phases/02-signal-rack-dashboard-redesign/02-04-SUMMARY.md` when done.
</output>
