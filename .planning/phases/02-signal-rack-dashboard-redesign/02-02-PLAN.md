---
phase: 02-signal-rack-dashboard-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/scrollBoxProbe.ts
autonomous: true
requirements: [DASH-10]
must_haves:
  truths:
    - "It is decided and recorded whether @opentui/core@0.2.4 exposes ScrollBox as a callable factory usable inline in the render tree the way Box()/Text() are"
    - "The dock task (Plan 05) has an unambiguous instruction: use ScrollBox factory OR keep hand-rolled scroll math"
  artifacts:
    - path: "src/dashboard/scrollBoxProbe.ts"
      provides: "A tiny type-checked probe importing/constructing ScrollBox to prove the usage shape; deleted or kept per outcome"
  key_links:
    - from: "Plan 05 dock log body"
      to: "ScrollBox factory vs hand-rolled visibleLogContent"
      via: "decision recorded in this plan's SUMMARY"
      pattern: "ScrollBox"
---

<objective>
Resolve 02-RESEARCH.md Open Question 2 before the dock is built: confirm whether `ScrollBox` in
`@opentui/core@0.2.4` is a callable factory (`ScrollBox({...opts}, ...children)`) matching how `Box()`/`Text()`
are used in `opentuiV3.ts`, or whether the log body must fall back to the existing hand-rolled scroll math.
`ScrollBox` is present in the package's top-level export list, but factory-vs-class usage is unconfirmed —
this spike gates the Plan 05 dock log implementation.

Purpose: Prevent Plan 05 from committing task tickets to an API shape that may not exist.
Output: A recorded decision (in this plan's SUMMARY) + a throwaway type-checked probe file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-signal-rack-dashboard-redesign/02-RESEARCH.md
@src/dashboard/opentuiV3.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe the ScrollBox export shape and record the decision</name>
  <files>src/dashboard/scrollBoxProbe.ts</files>
  <read_first>
    - 02-RESEARCH.md Pattern 3 + Open Question 2 + Assumption A3 (factory vs `new ScrollBoxRenderable(ctx, options)`)
    - src/dashboard/opentuiV3.ts lines 1-11 (how Box/Text/StyledText are imported from "@opentui/core")
    - node_modules/@opentui/core/renderables/ScrollBox.d.ts (ScrollBoxOptions/ScrollBoxRenderable API: stickyScroll, stickyStart, scrollTop, scrollHeight)
    - node_modules/@opentui/core/index.js (grep the export list — ScrollBox is present around the Box/Text exports)
  </read_first>
  <action>
    Grep `node_modules/@opentui/core/index.js` and the compiled dist for how `ScrollBox` is exported (factory function vs
    class re-export), mirroring how `Box` is defined. Create `src/dashboard/scrollBoxProbe.ts` that imports `ScrollBox`
    from `@opentui/core` and writes the smallest expression that proves the usable call shape — either a factory call
    `ScrollBox({ stickyScroll: true, stickyStart: "bottom", scrollY: true }, Text({ content: "x" }))` or a
    `new ScrollBoxRenderable(...)` construction — whichever type-checks. Run the type-checker to confirm. In the SUMMARY,
    record the verdict as one of: "FACTORY — Plan 05 uses ScrollBox({...opts}, ...children)" or
    "CLASS/UNAVAILABLE — Plan 05 keeps hand-rolled visibleLogContent/withScrollbar/logProgressLine and adds their missing unit tests".
    Do NOT wire the probe into the dashboard render tree. Either delete `scrollBoxProbe.ts` after recording the verdict or
    leave it as a compile-only reference; note which in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY states one unambiguous verdict (FACTORY or CLASS/UNAVAILABLE) with the exact call/construction form Plan 05 must use
    - `npx tsc -p tsconfig.json --noEmit` passes for the chosen probe expression (or, if ScrollBox is unusable, the probe is removed and the fallback verdict is recorded)
    - No import of scrollBoxProbe from opentuiV3.ts or any shipped module
  </acceptance_criteria>
  <done>Plan 05's dock-log approach is decided and type-check-backed; no guesswork remains about the ScrollBox usage shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Spike introduces no runtime surface; probe is compile-only and not wired into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies — ScrollBox ships inside the already-pinned @opentui/core@0.2.4; no install task |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` clean
- SUMMARY carries the FACTORY vs CLASS/UNAVAILABLE verdict Plan 05 depends on
</verification>

<success_criteria>
- The ScrollBox usage shape is confirmed and recorded before the dock is built
- Plan 05 has a deterministic instruction (factory or hand-rolled fallback)
</success_criteria>

<output>
Create `.planning/phases/02-signal-rack-dashboard-redesign/02-02-SUMMARY.md` when done — it MUST contain the ScrollBox verdict.
</output>
