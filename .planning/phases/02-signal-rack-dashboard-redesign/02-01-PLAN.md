---
phase: 02-signal-rack-dashboard-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/model.ts
  - tests/dashboard.test.mjs
autonomous: true
requirements: [DASH-09]
must_haves:
  truths:
    - "The view model exposes contextPercent/contextLevel/compacted per peer row (undefined when unmeasured, not defaulted to 0/green)"
    - "Peer rows can be grouped into exactly 5 triage buckets in order WORKING → WAITING → STARTING → FAILED → DONE"
    - "A pure helper turns a context percent into a 10-cell block meter string and maps a contextLevel to a cyberpunkTheme color"
  artifacts:
    - path: "src/dashboard/model.ts"
      provides: "DashboardPeerRow context fields + triageGroups/triageBucketForStatus + contextMeterCells/contextLevelColor exported helpers"
      contains: "export function triageGroups"
    - path: "tests/dashboard.test.mjs"
      provides: "Unit assertions for context fields, triage grouping, and the context-meter helpers"
      contains: "triageGroups"
  key_links:
    - from: "src/dashboard/model.ts createDashboardViewModel rows.map"
      to: "PeerRecord.contextPercent/contextLevel/compacted"
      via: "field copy into DashboardPeerRow"
      pattern: "contextPercent"
---

<objective>
Establish the pure, terminal-free view-model foundation DASH-09 needs: thread the already-computed
`contextPercent`/`contextLevel`/`compacted` fields from `PeerRecord` into `DashboardPeerRow`, add a
5-bucket triage-order grouping helper (distinct from the current 15-status `STATUS_ORDER`), and add a
pure context-meter helper (10-cell block string + level→color mapping). All three are unit-testable in
`tests/dashboard.test.mjs` without a live OpenTUI renderer.

Purpose: The rack renderer (Plan 04) and dock detail (Plan 05) consume these; keeping the logic in
`model.ts` makes it assertable per 02-VALIDATION.md's per-req test map.
Output: Extended `model.ts` exports + new assertions in `tests/dashboard.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-RESEARCH.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-VALIDATION.md
@.claude/skills/sketch-findings-delamain/references/layout-and-density.md
@src/dashboard/model.ts
@src/dashboard/theme.ts
@src/codexContext.ts
@src/types.ts
@tests/dashboard.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread context fields into DashboardPeerRow</name>
  <files>src/dashboard/model.ts, tests/dashboard.test.mjs</files>
  <read_first>
    - src/dashboard/model.ts lines 24-38 (DashboardPeerRow type) and 106-120 (rows.map in createDashboardViewModel)
    - src/types.ts lines 89-96 (PeerRecord.contextPercent/contextLevel/compacted, "Absent until first token_count event")
    - src/codexContext.ts lines 5, 46 (CodexContextLevel type, contextLevel thresholds)
    - 02-RESEARCH.md Pitfall 3 + Pitfall 4 (fields optional/often undefined; must thread through createDashboardViewModel, not the renderer)
  </read_first>
  <behavior>
    - createDashboardViewModel copies peer.contextPercent/contextLevel/compacted onto each DashboardPeerRow verbatim
    - When PeerRecord has no context data (early lifecycle / cursor engine), the row fields are undefined — NOT 0 / "green" / false-coerced
    - A compacted peer surfaces compacted === true on its row
  </behavior>
  <action>
    Add optional `contextPercent?: number`, `contextLevel?: CodexContextLevel`, and `compacted?: boolean` to the
    `DashboardPeerRow` type. Import `CodexContextLevel` from `../codexContext.js`. In the `rows.map(...)` block of
    `createDashboardViewModel`, populate the three fields directly from `peer` with no defaulting (per Pitfall 3 —
    defaulting the display would misrepresent an unmeasured peer as safe). Do not touch the existing fields.
    In tests, extend an existing `createDashboardViewModel` fixture (or add one) with a codex peer carrying
    contextPercent/contextLevel/compacted and assert they appear on the row; add a second peer with the fields
    absent and assert the row fields are `undefined`.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardPeerRow` declares contextPercent/contextLevel/compacted as optional
    - A test asserts a measured codex peer's row carries the exact contextPercent/contextLevel/compacted values
    - A test asserts an unmeasured peer's row has those three fields strictly `=== undefined`
    - `npx tsc -p tsconfig.json --noEmit` passes; `node --test tests/dashboard.test.mjs` passes
  </acceptance_criteria>
  <done>The view model carries per-row context telemetry, undefined-safe, with assertions proving both the present and absent cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5-bucket triage grouping helpers</name>
  <files>src/dashboard/model.ts, tests/dashboard.test.mjs</files>
  <read_first>
    - src/dashboard/model.ts lines 6-8 (DashboardStatus), 77-93 (STATIC_ACTIVITY covers all GSD statuses), 177-186 (dashboardPeers/dashboardStatus), 520-540 (statusRank)
    - .claude/skills/sketch-findings-delamain/references/layout-and-density.md lines 5-7 (triage order WORKING → WAITING → STARTING → FAILED → DONE; row glyphs)
    - 02-RESEARCH.md Pattern 1 + Assumption A1 (fold GSD statuses into nearest of the 5 buckets; do NOT invent new buckets)
  </read_first>
  <behavior>
    - triageBucketForStatus maps every DashboardStatus to one of exactly 5 buckets: "working" | "waiting" | "starting" | "failed" | "done"
    - GSD statuses fold: working-like (gsd_running_phase/gsd_polling_state/gsd_running_gate_check) → working; halted/failed-like (failed/frozen/gsd_halted_on_gate_failure/gsd_failed/killed) → failed; completed/terminal-like (done/cleanup/gsd_completed/idle/gsd_pending) → done; waiting → waiting; starting → starting
    - triageGroups returns the 5 buckets in fixed triage order, each with its member rows, omitting empty buckets (or flagged empty — pick one and assert it)
  </behavior>
  <action>
    Add and export `triageBucketForStatus(status: DashboardStatus): TriageBucket` and
    `triageGroups(rows: DashboardPeerRow[]): Array&lt;{ bucket: TriageBucket; label: string; peers: DashboardPeerRow[] }&gt;`
    where `TriageBucket` is the 5-value union above and the fixed order array is `["working","waiting","starting","failed","done"]`.
    Reuse the existing sort from `dashboardPeers` implicitly (rows arrive pre-sorted); within a bucket preserve incoming
    order. Do NOT reorder or remove `STATUS_ORDER` in this plan (that is opentuiV3.ts's concern in Plan 04). Add tests
    asserting: every DashboardStatus maps to a valid bucket, the GSD fold mapping from A1, and that triageGroups yields
    buckets in the exact WORKING→WAITING→STARTING→FAILED→DONE order.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function triageGroups" src/dashboard/model.ts` returns 1
    - A test enumerates all DashboardStatus values and asserts each maps to one of the 5 buckets
    - A test asserts triageGroups output order is exactly working, waiting, starting, failed, done
    - Full `node --test tests/dashboard.test.mjs` passes
  </acceptance_criteria>
  <done>Peer rows group into the 5 sketch-locked triage buckets in order, with GSD statuses folded per A1 and unit-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Context-meter block + color helpers</name>
  <files>src/dashboard/model.ts, tests/dashboard.test.mjs</files>
  <read_first>
    - src/dashboard/theme.ts lines 51-82 (cyberpunkTheme: statusColors.starting #35e0d8, text #ffb066, accent #ff7a1a, statusColors.failed #ff4433)
    - .claude/skills/sketch-findings-delamain/references/layout-and-density.md line 8 (10-cell block meter `████░░░░░░ 42%`; green #35e0d8, yellow #ffb066, red #ff7a1a, skull #ff4433)
    - 02-RESEARCH.md Pattern 2 + Assumption A2 (green == teal accent, reuse theme keys not new hexes)
    - src/codexContext.ts line 46 (contextLevel thresholds)
  </read_first>
  <behavior>
    - contextMeterCells(50) → "█████░░░░░" (10 chars, round(50/100*10)=5 filled)
    - contextMeterCells(0) → "░░░░░░░░░░"; contextMeterCells(100) → "██████████"
    - contextMeterCells clamps out-of-range percents to 0..100 before computing
    - contextLevelColor maps green→#35e0d8, yellow→#ffb066, red→#ff7a1a, skull→#ff4433 (sourced from theme keys, not literals where a theme key already holds the value)
  </behavior>
  <action>
    Add and export `contextMeterCells(percent: number): string` building `"█".repeat(filled) + "░".repeat(10 - filled)`
    with `filled = Math.round(clamp(percent,0,100)/100*10)`, and `contextLevelColor(level: CodexContextLevel, theme: Theme): string`
    returning green→theme.statusColors.starting, yellow→theme.text, red→theme.accent, skull→theme.statusColors.failed
    (these cyberpunkTheme keys already equal the sketch hexes per A2 — verify against theme.ts before hardcoding). Keep the
    skull-blink timing OUT of this helper (the renderer owns nowMs animation); this helper is pure and time-independent.
    Add tests for the three fixed cell outputs above, clamping, and each level→color mapping under cyberpunkTheme.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `contextMeterCells(42)` returns a 10-character string with 4 filled `█` and 6 `░`
    - A test asserts contextMeterCells clamps a &gt;100 and a &lt;0 input to full/empty
    - A test asserts contextLevelColor("green", cyberpunkTheme) === "#35e0d8" and contextLevelColor("skull", cyberpunkTheme) === "#ff4433"
    - Full suite `node --test tests/dashboard.test.mjs` passes
  </acceptance_criteria>
  <done>Pure, time-independent context-meter helpers exist and are unit-proven against the sketch spec and cyberpunkTheme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer session JSONL → view model | Untrusted peer-emitted context telemetry is copied into DashboardPeerRow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | contextMeterCells / row context fields | mitigate | Never default undefined telemetry to 0/green (Pitfall 3) — an unmeasured peer must not render as "safe"; helpers clamp numeric range only |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies in this phase (Standard Stack: "Package Legitimacy Audit not applicable"); no install tasks present |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` clean
- `npm run build && node --test tests/dashboard.test.mjs` green (per 02-VALIDATION.md sampling: type-check + dashboard subset per task)
- No renderer/OpenTUI import added to model.ts (stays terminal-free)
</verification>

<success_criteria>
- DashboardPeerRow carries undefined-safe contextPercent/contextLevel/compacted
- triageGroups/triageBucketForStatus produce the 5 sketch buckets in order with GSD statuses folded
- contextMeterCells/contextLevelColor match the sketch spec, all unit-proven
- Full dashboard test suite passes
</success_criteria>

<output>
Create `.planning/phases/02-signal-rack-dashboard-redesign/02-01-SUMMARY.md` when done.
</output>
