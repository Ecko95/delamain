---
phase: 02-signal-rack-dashboard-redesign
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/dashboard/opentuiV3.ts
autonomous: false
requirements: [DASH-10]
must_haves:
  truths:
    - "A fixed bottom dock shows the selected peer's detail plus a scrollable log that follows the tail by default"
    - "The dock title shows a `log N/M ▼ tail / ▲ scrolled` position indicator; scrolling up detaches, b jumps to tail, and changing selection re-attaches the tail"
    - "Tab moves focus between the rack and the dock; the focused pane's border is glow-highlighted teal"
    - "kill-confirm and answer modes render in a status line above the footer (not a modal), and the footer keybar reflects the focused pane"
    - "The centered peer modal (modalBox/modalTabBody/modalButtonRow/modalReveal) is deleted"
  artifacts:
    - path: "src/dashboard/opentuiV3.ts"
      provides: "Bottom dock (detail + tail-follow log + position indicator), statusLine, focus-aware footer, dashboardState wiring; modal removed"
      contains: "statusLine"
  key_links:
    - from: "opentuiV3.ts dashboardState"
      to: "createDashboardViewModel message/mode"
      via: "state.mode passed through instead of hardcoded normal"
      pattern: "mode: state.mode"
---

<objective>
Land the DASH-10 render surface: replace `logsDrawer` + the centered modal with the sketch-locked fixed bottom dock
(selected-peer detail + scrollable, tail-following log + `log N/M ▼ tail / ▲ scrolled` indicator), render the kill-confirm/
answer modes (from Plan 03) in a new `statusLine()` above the footer, make the footer keybar reflect the focused pane,
glow-highlight the focused pane's border teal, and fix `dashboardState()` so it passes `state.mode` through instead of
hardcoding `"normal"`. Uses ScrollBox or the hand-rolled fallback per Plan 02's verdict.

Purpose: Deliver DASH-10 Success Criteria 3 and 4, and close the `dashboardState()` wiring gap that currently prevents
`messageForState()` from ever showing.
Output: Reworked dock/status/footer in `opentuiV3.ts`, ~360 lines of modal/scroll code deleted. Verified by type-check,
full suite, and a human smoke check of tail-follow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-RESEARCH.md
@.planning/phases/02-signal-rack-dashboard-redesign/02-VALIDATION.md
@.claude/skills/sketch-findings-delamain/references/keyboard-and-detail-dock.md
@.claude/skills/sketch-findings-delamain/sources/002-signal-rack-terminal/index.html
@.planning/phases/02-signal-rack-dashboard-redesign/02-02-SUMMARY.md
@src/dashboard/opentuiV3.ts
@src/dashboard/model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixed bottom dock — detail + tail-following log + position indicator; retire the modal</name>
  <files>src/dashboard/opentuiV3.ts</files>
  <read_first>
    - .planning/phases/02-signal-rack-dashboard-redesign/02-02-SUMMARY.md (ScrollBox verdict: FACTORY vs CLASS/UNAVAILABLE — dictates this task's log-body approach)
    - src/dashboard/opentuiV3.ts lines 234-302 (computeLayout drawerH + render() composition incl. modal branch), 633-688 (inspectorPane/inspectorRow/wrapText — detail rows to fold into the dock), 850-907 (logsDrawer/logLineChunks), 982-1156 (modalBox/modalTabBody/modalButtonRow — to delete), 1320-1327 (modalReveal), 1366-1418 (visibleLogContent/logProgressLine/withScrollbar/scrollPosition/clamp)
    - .claude/skills/sketch-findings-delamain/references/keyboard-and-detail-dock.md lines 6-12, 20-40 (fixed ~16-row dock, tail-follow rules, `log N/M ▼ tail / ▲ scrolled` indicator, re-attach on selection change, b jumps to bottom)
    - 02-RESEARCH.md Pattern 3 + Anti-Patterns (no modal; delete modalBox family) + State of the Art (ScrollBox stickyStart bottom)
  </read_first>
  <action>
    Replace `logsDrawer` with a `dockPane` that is a fixed-height pane (~16 rows via computeLayout) with two regions: a compact
    selected-peer detail block (reuse `view.details`/`inspectorRow`/`wrapText` content, not a floating modal) and a scrollable log
    body. If Plan 02's verdict is FACTORY, build the log body with `ScrollBox({ stickyScroll: true, stickyStart: "bottom", scrollY: true, height, width: "100%" }, ...one Text per view.logLines line)` and derive the `log N/M ▼ tail / ▲ scrolled`
    indicator from the ScrollBox's scrollTop/scrollHeight (▼ tail when at bottom, ▲ scrolled otherwise); re-attach to tail on selection
    change (selection change already resets logOffset in v3Input) and on `b`. If the verdict is CLASS/UNAVAILABLE, keep the existing
    `visibleLogContent`/`withScrollbar`/`scrollPosition` math and render the same `log N/M ▼ tail / ▲ scrolled` indicator from
    offset/visibleRows/totalRows (▼ tail when offset === 0). Delete `modalBox`, `modalTabBody`, `modalButtonRow`, `modalReveal`, and the
    modal/palette-independent branch in `render()` (lines ~293-299) that adds the modal; keep the palette and help overlays. Update
    `computeLayout` so the dock is a fixed pane (drop the collapsible `drawerOpen` 1-row state if it conflicts with "always-visible dock",
    or keep ` toggle) — match the sketch's always-visible dock).
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "modalBox" src/dashboard/opentuiV3.ts` returns 0 (modal family deleted)
    - The dock renders selected-peer detail rows AND a scrollable log body with a `log N/M ▼ tail` / `▲ scrolled` indicator string
    - `npx tsc -p tsconfig.json --noEmit` and the full suite pass
  </acceptance_criteria>
  <done>The bottom dock replaces the modal + drawer with detail + a tail-following scrollable log and a position indicator per the sketch.</done>
</task>

<task type="auto">
  <name>Task 2: Status line, focus glow, focus-aware footer, and dashboardState wiring</name>
  <files>src/dashboard/opentuiV3.ts</files>
  <read_first>
    - src/dashboard/opentuiV3.ts lines 223-232 (dashboardState — HARDCODES mode: "normal"), 279-302 (render composition), 494-546 (rosterPane border/focus treatment), 850-888 (dock border focus), 911-938 (footer keybar)
    - src/dashboard/model.ts lines 289-303 (messageForState — already yields the kill-confirm/answer text) and DashboardMode
    - .claude/skills/sketch-findings-delamain/references/keyboard-and-detail-dock.md lines 13-14 (kill-confirm red+glow `kill <id>? ↵ confirm · esc cancel`; answer teal inline `reply → <id>: [input]`; footer reflects focused pane)
    - 02-RESEARCH.md Pitfall 2 + "Existing status-line message the render layer currently ignores" (dashboardState wiring gap is the actual DASH-10 fix)
  </read_first>
  <action>
    Fix `dashboardState()` to pass `mode: state.mode` (mapped to the model.ts DashboardMode: "kill-confirm"/"answer"/"help"/"normal")
    and the current focus (rack vs dock) through to `createDashboardViewModel`, so `view.message` reflects the real mode. Add a
    `statusLine()` render function placed above the footer that renders `view.message`: kill-confirm in danger red `#ff4433` with glow,
    answer mode in teal `#35e0d8` showing the live `state.answerInput`, normal → the ambient/ready message. Insert it into `render()`'s
    children between the dock and the footer. Generalize the focus-glow: set the border of BOTH the rack pane and the dock to
    `theme.borderFocused` (teal) when that pane holds focus (derive from the existing `state.drawerFocused` two-pane toggle;
    `false` = rack focused, `true` = dock focused) and `theme.border` otherwise. Update `footer()` so the keybar text reflects the focused
    pane (e.g. dock focused → `↑↓/jk scroll log · b tail · tab focus rack`; rack focused → `↑↓/jk move · tab focus dock`). Keep the LIVE/
    SCROLLED status indicator.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npm run build &amp;&amp; node --test tests/dashboard.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mode: "normal"' src/dashboard/opentuiV3.ts` returns 0 in dashboardState (mode is passed through, not hardcoded)
    - `grep -c "statusLine" src/dashboard/opentuiV3.ts` returns &gt;= 1 and it is added to render()'s children
    - The focused pane (rack or dock) uses theme.borderFocused; the unfocused pane uses theme.border
    - The footer keybar text differs between rack-focused and dock-focused states
    - `npx tsc -p tsconfig.json --noEmit` and the full suite pass
  </acceptance_criteria>
  <done>Kill-confirm/answer render in a status line, dashboardState passes the real mode through, and Tab focus glow + focus-aware footer work per the sketch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke check — dock tail-follow, focus, status-line modes</name>
  <read_first>
    - .claude/skills/sketch-findings-delamain/references/keyboard-and-detail-dock.md (tail-follow + indicator + status-line spec)
    - 02-VALIDATION.md Manual-Only Verifications (live renderer scroll state has no headless assertion path)
  </read_first>
  <what-built>
    Fixed bottom dock with selected-peer detail + tail-following scrollable log + `log N/M ▼ tail / ▲ scrolled` indicator,
    Tab focus glow between rack and dock, a status line rendering kill-confirm (red) and answer (teal) modes, and a
    focus-aware footer keybar. Centered modal removed.
  </what-built>
  <how-to-verify>
    1. Run `CODEX_PEERS_DASHBOARD_SMOKE=1 bun src/dashboard/bunEntryV2.js` (or interactive `bun` run) with a peer that has log output.
    2. Confirm the dock shows the selected peer's detail and its log; the log follows the newest lines (`▼ tail`).
    3. Scroll up (j/k or wheel with dock focused): indicator flips to `▲ scrolled`; press `b`: it jumps back to `▼ tail`.
    4. Change selection (j/k with rack focused): the dock re-attaches to the tail for the new peer.
    5. Press Tab: focus/glow moves between rack and dock (teal border); the footer keybar text changes to match.
    6. Press `x` on a peer: status line shows `kill <id>? ↵ confirm · esc cancel` in red; esc cancels. On a waiting peer press `a`: status line shows the teal inline reply prompt; type + enter sends, esc cancels. Confirm no centered modal ever appears.
  </how-to-verify>
  <acceptance_criteria>
    - Tail-follow, detach-on-scroll-up, b-jumps-to-tail, and re-attach-on-selection-change all behave per the sketch
    - Tab glow + focus-aware footer work
    - kill-confirm/answer render in the status line (red/teal); no modal appears
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer log lines → dock log render | Untrusted peer log content rendered verbatim in the scrollable dock |
| operator keyboard → answer text (status line) | Free-text reply displayed and submitted from the status line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Tampering (ASVS V5) | answer status-line input | mitigate | Answer text still flows through Plan 03's `.trim()`+empty-reject `submitAnswer`; the status line only renders `state.answerInput`, it does not bypass validation |
| T-2-05 | Tampering/Info Disclosure | dock log line rendering | accept | Pre-existing — reuses the same view.logLines/LogBuffer pipeline and OpenTUI Text escaping as today; no new handling (Security Domain) |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; ScrollBox ships in the pinned @opentui/core; no install task |
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` clean
- `npm run test` green — full suite (`tests/*.test.mjs`, incl. MCP/CLI/peer-supervision), not just the dashboard subset (Success Criterion 6 regression guard)
- Human smoke check (Task 3) approved: tail-follow, focus glow, status-line modes, no modal
</verification>

<success_criteria>
- Fixed bottom dock shows detail + tail-following log with the `log N/M ▼ tail / ▲ scrolled` indicator; b jumps to tail; selection re-attaches
- Tab cycles rack↔dock focus with teal glow; footer keybar reflects focus
- kill-confirm/answer render in the status line; centered modal deleted; dashboardState passes the real mode through
- Existing suite still passes
</success_criteria>

<output>
Create `.planning/phases/02-signal-rack-dashboard-redesign/02-05-SUMMARY.md` when done.
</output>
