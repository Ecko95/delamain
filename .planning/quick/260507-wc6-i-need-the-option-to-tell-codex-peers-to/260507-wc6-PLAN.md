---
quick_id: 260507-wc6
slug: i-need-the-option-to-tell-codex-peers-to
status: in-progress
created: "2026-05-07T21:17:01.961Z"
---

# Quick Task 260507-wc6: codex-peers configurable worktree routing

## Goal

Allow codex-peers spawns to choose a separate worktree start point and origin merge target, while keeping origin main/master defaults and backwards-compatible target branch behavior. Add a reusable project skill that prompts the orchestrator for the peer to spawn, the worktree start ref, and the merge branch, then asks for confirmation before spawning.

## Tasks

1. Update peer spawn types, MCP schema, CLI flags, runner prompt, and state so `start_ref`/`--start-ref` controls the worktree base and `merge_branch`/`--merge-branch` controls the integration target.
   - Files: `src/types.ts`, `src/git.ts`, `src/peerManager.ts`, `src/runner.ts`, `src/mcpServer.ts`, `src/cli.ts`, dashboard model if needed.
   - Verify: TypeScript check and existing lifecycle behavior still compile.

2. Add regression tests for start-ref and merge-branch separation, including local refs and backwards-compatible `target_branch`.
   - Files: `tests/git.test.mjs`.
   - Verify: `npm test`.

3. Add the orchestrator prompting skill and README usage notes.
   - Files: `.codex/skills/codex-peers-worktree-routing/SKILL.md`, `README.md`.
   - Verify: skill frontmatter is valid and README matches actual options.
